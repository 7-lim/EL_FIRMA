{# templates/categorie/edit.html.twig #}
{% extends 'baseFournisseur.html.twig' %}

{% block body %}
<div class="container mt-5">
    <div class="card border-0 shadow-lg">
        <div class="card-body">
            <h3 class="text-success mb-4">Modifier la catégorie</h3>
            {{ include('categorie/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            <a href="{{ path('app_categorie_index') }}" class="btn btn-outline-secondary mt-3">Retour</a>
        </div>
    </div>
</div>
{% endblock %}
