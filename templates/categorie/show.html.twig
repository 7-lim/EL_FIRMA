<h2>{{ categorie.name }}</h2>

<ul>
    {% for produit in produits %}
        <li>{{ produit.name }}</li>
    {% else %}
        <li>No products in this category.</li>
    {% endfor %}
</ul>

<!-- Back to categories list -->
<a href="{{ path('app_categorie_index') }}">Back to Categories</a>
